import styled from '@emotion/styled'

const HorizontalRule = styled.div`
  border: 0;
  height: 0.0625rem;
  background-image: linear-gradient(
    to right,
    rgba(0, 0, 0, 0),
    rgba(0, 0, 0, 0.25),
    rgba(0, 0, 0, 0)
  );
  margin-top: 5%;
`

export default {
  HorizontalRule,
}
